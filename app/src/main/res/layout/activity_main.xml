<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.gaut.appbletooth.MainActivity">

    <TextView
        android:id="@+id/nom_app_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Application bluetooth" />

    <Button
        android:id="@+id/bluetooth_button"
        android:layout_width="wrap_content"
        android:layout_height="75dp"
        android:text="BLUETOOTH"
        android:layout_below="@+id/nom_app_textview" />

    <Button
        android:id="@+id/scan_button"
        android:layout_width="wrap_content"
        android:layout_height="75dp"
        android:text="SCAN"
        android:layout_toRightOf="@+id/bluetooth_button"
        android:layout_below="@+id/nom_app_textview"  />

    <ListView
        android:id="@+id/device_ListView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_animation"
        android:textColor="@android:color/black"/>

    <ImageView
        android:id="@+id/iv_animation"
        android:adjustViewBounds="true"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/scan_button"
        android:contentDescription="Animation"/>


</RelativeLayout>
